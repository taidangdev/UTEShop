import React from 'react';

/**
 * @deprecated
 * Toast notifications are now handled globally by NotificationProvider in context/NotificationContext.tsx.
 * This component is kept to prevent import errors and returns null.
 */
export const ToastNotification: React.FC = () => null;
